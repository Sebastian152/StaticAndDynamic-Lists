#ifndef TEMPLATE_HPP
#define TEMPLATE_HPP

class Template {
    private: 
        int number;

    public: 
        Template();
        Template(int number);
        int getNumber();
        void setNumber(int number);
};

#endif // TEMPLATE_HPP